#include <bits/stdc++.h>
using namespace std;

int solve(int n, vector<vector<int>>& adj, int visited[]){
    queue<int> q;
    q.
}

int main(){
    // say we have a adjacency list and that is adj
    vector<vector<int>> adj;

    int n; // n is the no of nodes 
    cin >> n;
    // make a visited array
    int visisted[n + 1] = {0}; // since 1 based indexing
    int provinces;

    solve(n, adj, visisted);
    return 0;
}