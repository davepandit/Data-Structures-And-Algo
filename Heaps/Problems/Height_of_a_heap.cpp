// User function Template for C++

class Solution {
  public:
    int heapHeight(int N, int arr[]) {
        // because we know that a heap is a complete binary tree 
        // so the height of the tree will be log n 
        
        int height = log2(N);
        
        return height;
        
    }
};