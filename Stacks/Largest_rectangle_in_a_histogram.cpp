#include <bits/stdc++.h>
using namespace std;

class Demo{
    
};

int max_area(vector<int> arr){
    int n = arr.size();
    
}

int main(){
    vector<int> hist_arr = {2, 1, 5, 6, 2, 3};
    int ans = max_area(hist_arr);
    return 0;
}